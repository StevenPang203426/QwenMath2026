# ============================================================
# CoT GRPO reward 修复 smoke: strict
# 从 sft_cot/best 开始，小步数验证严格 <answer> 约束是否改善格式与准确率。
# ============================================================
inherit: grpo

reward:
  variant: "strict"

training:
  output_dir: "outputs/checkpoints/grpo_cot_reward_strict_smoke"
  max_steps: 50
  num_train_epochs: 1
  per_device_train_batch_size: 4
  gradient_accumulation_steps: 1
  learning_rate: 5.0e-6
  logging_steps: 1
  save_steps: 25
  save_total_limit: 2
  max_grad_norm: 0.1

grpo:
  num_generations: 4
  max_completion_length: 256
  max_prompt_length: 256
  temperature: 0.8
  top_p: 0.95
  use_vllm: true
  vllm_mode: "colocate"
  vllm_gpu_memory_utilization: 0.3
  generation_batch_size: 4

logging:
  run_name: "grpo-cot-reward-strict-smoke"
  tags: ["grpo", "cot", "reward-strict", "smoke"]
