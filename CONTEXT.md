# Context Glossary

## 规范表达式
只由数字和纯算术运算构成的表达式。它不能包含方程、比较、条件分支、函数调用、变量、单位、LaTeX 或计算机取巧语法。

## 题意归一
先计算表达式得到原始数值，再根据题目要求转换成最终答案格式，例如百分数、分数、至少/至多取整或保留小数。

## 安全表达式数据
通过规范表达式检测、可安全计算，并且题意归一后与标注答案一致的数据。

## 表达式正样本
模型应学习输出的高可信表达式样本。它必须包含规范表达式、题目和标注答案，并且表达式经题意归一后与标注答案一致。

## 表达式偏好对
用于偏好优化的一组表达式回答。chosen 是正确的表达式回答，rejected 是可解析但题意归一后答案错误的表达式回答。

## 表达式路线
让模型先输出规范表达式，再通过表达式计算和题意归一得到最终答案的训练与推理路线。

## 安全合并
只把安全表达式数据、已验证修复数据和已审计增强数据纳入训练集；无法证明安全的数据进入拒绝记录。

## 表达式基底投票
推理融合时以安全表达式候选为主要答案来源，思维链答案只用于验证、破局或兜底。

## 思维链验证
使用思维链模型的答案检查或辅助选择表达式候选，但不覆盖高置信表达式多数结果。
