# Submission Outputs

This directory is for generated submission CSV/JSON files. They are intentionally ignored by git so local runs do not pollute commits.

Keep only `.gitkeep` and this README tracked. Recreate submissions with:

```bash
bash scripts/submit.sh infer sft_cot
bash scripts/submit.sh expr4
```
